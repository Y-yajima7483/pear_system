# デプロイ安全契約（公開用・最小版）

このファイルはリリース時に守る最小限の契約であり、運用正本ではない。
実ホスト名、アカウント、ファイル配置、プロバイダー設定、認証情報、復旧手順、
作業者向けの詳細手順は、Git外の非公開運用正本で管理する。

## 構成の原則

- 正規リリース対象はネイティブホスト構成とする。
- ブラウザ、Next.js、Laravelは同一のHTTPS originを使う。
- `/` はNext.js、`/api/*` と `/sanctum/*` はLaravelへ転送する。
- Dockerおよび外部エッジ経由の構成は代替案であり、正規構成の検証を代替しない。

## 設定と秘密情報

- イメージとビルド成果物に `.env`、鍵、認証情報、DBダンプ、ログ、
  キャッシュ、非公開運用文書を含めない。
- 秘密値は非公開の実行環境からruntimeに注入する。
- Git管理する `*.example` は変数名と非機密の例だけを持つ。
- 本番ではAPP key、DB認証情報、HTTPS cookie、限定したSanctumホストと
  CORS originを必須にする。
- ブラウザからのAPIは同一originの相対パスを使う。公開ビルド変数は
  メタデータ用のoriginに限定し、秘密値を渡さない。

## リリースゲート

1. デプロイ排他ロックとDB共通maintenance lockを取得し、対象revisionを一意に固定する。
2. DB変更前にビルドと静的検査を完了する。
3. migrationの直前に新しいbackupを作成し、整合性を検証する。
4. migrationは明示的なリリース工程で一度だけ実行し、アプリ起動時は自動実行しない。
5. migration成功後にだけサービスを再読み込みする。
6. health、Sanctum CSRF cookie、login、認証必須API、logoutのsmoke testを必須とする。
   一つでも失敗した場合、リリースは失敗と判定する。

本番Seederは禁止する。backup保持、オフホスト複製、restore、rollback、
インシデント対応の実値と手順は非公開運用正本で定義・定期検証する。
