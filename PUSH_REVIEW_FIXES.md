# Push前監査の修正・再確認事項

> **記録の位置づけ:** 以下は修正前時点の監査記録です。件数、NO-GO結論、
> 旧migration方針は履歴であり、現在のGO/NO-GO判定を示すものではありません。

## 1. 監査概要

- 監査日時: 2026年7月16日（Asia/Tokyo）
- 対象: `main` ブランチの作業ツリー、ステージ済み差分、未追跡ファイル、および `origin/main` との差分
- 目的: リモートへpushする前に、機密情報、既存DB破損、ビルド不能、API契約不整合、データ損失、本番設定不備などを確認する
- 結論: **NO-GO（現状のままcommit・pushしない）**

未リリースのシステムである点は考慮したが、他の開発者がpullした際の既存DB破損、保存失敗の誤認、本番環境でのAPI・認証不能、環境ファイルのイメージ混入など、push前に解消すべき問題が残っている。

## 2. 監査時点のGit・リモート状態

- 現在のブランチ: `main`
- ローカルHEAD: `origin/main` と同一
- 未pushコミット: 0件
- staged: 6件
- unstaged: 56件
- untracked: 31件
- リモート接続: SSH認証に失敗しており、リモートへのpush可否は未確認

特に、ステージ済み差分は作業ツリー全体を反映していない。rename-onlyの状態でステージされているファイルがあり、クラス名、ルート、利用側などの対応変更が未ステージのため、現在のindexをそのままcommitすると壊れたコミットになる。

## 3. 修正ポイント

### P0: pushを必ず止める問題

#### 3.1 適用済みマイグレーションのファイル名変更

- `create_ varieties_table` を含む既存マイグレーションが、空白を除いた別名へrenameされている。
- Laravelはマイグレーション名を実行履歴として管理するため、既存DBではrename後のファイルを未実行と判断する。
- その状態で `php artisan migrate` を実行すると、既に存在する `varieties` テーブルを再作成しようとして失敗する可能性が高い。

対応:

- 既存ファイル名を維持するか、既存DBでも安全に移行できる方法へ変更する。
- 少なくとも、既存の開発DBに対する通常の `migrate` が成功することを確認する。
- `migrate:fresh` を前提にする場合は、開発データ破棄を伴うため、関係者へ明示して合意を得る。

#### 3.2 `direct_sale_products` のUNIQUE制約変更

- 同一商品を玉数違いで複数行登録する仕様へ変更されている。
- しかしUNIQUE制約の削除は、既存のcreateマイグレーションを書き換えただけで、既存DBへ適用する追加マイグレーションがない。
- 新規DBでは複数行を登録できる一方、既存DBでは旧制約が残り、同じAPIリクエストが失敗するというスキーマ差異が生じる。

対応:

- 旧UNIQUE制約を安全に削除する追加マイグレーションを作成する。
- 新規DBと既存DBの両方で同一商品・複数行の登録テストを通す。
- 過去のcreateマイグレーションから価格列や合計列を削除している変更も、既存DBとのスキーマ差異が許容されるか確認する。

#### 3.3 直売APIのアプリケーションエラーを成功扱いする

- Laravelサービスは例外時にもHTTP 200で `success: false` を返す構造になっている。
- 直売登録・編集ダイアログは通信レベルの成功だけを確認し、レスポンス本文の `success` を確認せず、フォームを閉じて入力をリセットする。
- DB制約違反などで保存されていなくても、利用者には成功したように見え、入力内容も失われる。

対応:

- 通信成功と業務処理成功を分け、レスポンス本文の成功フラグを必ず確認する。
- 失敗時はダイアログと入力内容を維持し、エラーを表示する。
- JA保存など、同じAPIフックを利用する処理にも同様の判定漏れがないか横断確認する。

#### 3.4 直売編集による明細消失

- 直売upsertは、その日の直売明細を一度すべて削除し、商品明細から再構築する。
- 一方、Seederや既存データには、ロス、プレゼント、良など、商品明細を持たない直売等級が含まれる。
- 日別編集画面は商品明細だけをフォームへ復元するため、編集保存すると復元されなかった直売明細が消える。

対応:

- 商品明細を持たない直売等級を今後も扱うか、データモデルと業務仕様を確定する。
- 扱う場合は編集フォーム/APIで欠落なく往復できるようにする。
- 扱わない場合はSeeder、既存データの移行、等級スコープを新仕様と一致させる。
- 保存前後で、同日に存在する直売・JA明細が意図せず消えない回帰テストを追加する。

#### 3.5 本番Next.jsのAPI URL

- クライアント側API URLは公開環境変数を参照して組み立てている。
- 本番Dockerfileのビルドステージには、その環境変数をビルド時に渡す仕組みがない。
- Composeのrunner実行時に指定しても、ブラウザ向けコードへビルド時に埋め込まれる値には反映されず、不正なAPI URLになる可能性が高い。

対応:

- ビルド時ARG/ENVとして明示的に渡すか、同一オリジンの相対URLを使う設計へ統一する。
- クリーンな本番ビルド後、ブラウザからAPIへ正しいURLで接続できることを確認する。

#### 3.6 本番nginxでSanctumのCSRFエンドポイントに到達できない

- 開発用nginxは `/api` と `/sanctum` をLaravelへ転送するよう変更されている。
- 本番用nginxは `/api` のみをLaravelへ転送しており、`/sanctum/csrf-cookie` がNext.js側へ流れる。
- SanctumのCookie認証を利用するログインが本番で成立しない。

対応:

- 本番用nginxにもSanctum用ルーティングを追加する。
- CSRF Cookie取得、ログイン、認証必須API、ログアウトまで本番相当構成で確認する。

#### 3.7 `.dockerignore` 不在と `.env` の本番イメージ混入

- ビルドコンテキストに対する `.dockerignore` がない。
- PHP本番DockerfileがLaravelディレクトリ全体をCOPYするため、ローカルに存在する環境ファイル、依存物、不要な生成物がイメージへ入る可能性がある。
- 環境ファイルがある環境では機密値の混入、ない環境では `APP_KEY` 等が不足するため、ビルド・実行結果が環境依存になる。

対応:

- `.dockerignore` で環境ファイル、Git管理情報、依存ディレクトリ、ビルド生成物、バックアップ等を除外する。
- `APP_KEY`、DB接続、Cookie・Sanctum・CORS関連設定は、イメージへ焼き込まず実行環境から安全に注入する。
- 機密値を持たない本番用envサンプルを用意し、必須変数を文書化する。
- クリーンcloneから同じイメージを再現できることを確認する。

#### 3.8 未ステージ差分を含む不完全なindex

- staged 6件に対して、unstaged 56件、untracked 31件が残っている。
- renameされたPHPファイルは、index上では旧クラス名のまま新しいパスへ移動した状態を含み、PSR-4のファイル解決に失敗する可能性がある。
- 削除済み定数ファイルの利用側変更なども、同じコミットに含まれないとビルドが壊れる。

対応:

- 修正完了後に、含めるファイルを明示して再ステージする。
- `git diff --cached --check`、`git diff --cached --name-status`、`git diff --cached` でindexだけを再監査する。
- commit直前に、ステージ済みスナップショットで構文検査・型検査・テストを再実行する。

### P1: push前に修正・方針確定すべき問題

#### 3.9 `restore-db.sh` の引数処理

- `set -u` の有効化後に第1引数を直接参照している。
- 引数なしで実行すると、Usageを表示する前にunbound variableで異常終了する。

対応:

- 第1引数をデフォルト付きで受け取り、その後にUsage判定を行う。
- 引数なし、存在しないファイル、キャンセル、正常リストアの各経路を確認する。

#### 3.10 テストのDB前提と再現性

- FeatureテストはMySQLとシード済みマスタを前提としており、テスト単体では自己完結していない。
- DBが起動していない環境では、多数のテストが接続エラーになる。

対応:

- テスト用DBの起動・初期化・シード手順を明示する。
- 可能ならテスト側で必要データを生成し、既存の開発DB内容へ依存しない形にする。
- テスト実行が開発データを破壊しないことを確認する。

#### 3.11 本番起動時の自動マイグレーション

- 本番PHPエントリポイントはコンテナ起動時に強制マイグレーションを実行する。
- 複数コンテナの同時起動や、互換性のないマイグレーションが含まれた場合に、起動とDB変更が密結合になる。

対応:

- デプロイ時に一度だけ実行する運用へ分離するか、排他・バックアップ・失敗時復旧を含む方針を決める。
- 少なくとも今回の既存DB不整合を解消するまで、本番相当環境で自動実行しない。

### P2: 品質改善・確認事項

#### 3.12 ESLint警告

- ESLintはエラー0件だが、警告が60件ある。
- 今回の変更箇所にもEffect内の同期的なstate更新、Hook依存配列などの警告が含まれる。

対応:

- 今回追加・変更したファイルの警告を優先して解消する。
- 既存警告と新規警告を分け、今後増やさない基準を設ける。

#### 3.13 環境依存の絶対パス

- 非Docker用デプロイ・バックアップスクリプトには、特定の配置先や設定ファイル位置を前提とする絶対パスがある。
- 意図した運用環境では有効でも、他環境ではそのまま利用できない。

対応:

- 運用上固定するパスか、環境変数で上書き可能にするパスかを区別する。
- 前提となるディレクトリ、権限、外部コマンドをREADME等に記載する。

## 4. 機密情報監査結果と確認条件

### 監査結果

- 差分および未追跡ファイルから、実運用の秘密鍵、アクセストークン、APIキー、実パスワードなどの明確な機密値は検出されなかった。
- envサンプルのDBパスワード欄は変更必須のプレースホルダーへ置き換えられている。
- バックアップ実体は検出されず、バックアップ用ディレクトリには構造保持用ファイルのみが存在した。

### push前の確認条件

- `UserSeeder` には固定の開発用ログインアカウント情報がある。実在人物のアドレスや他環境で使う認証情報ではないことを確認し、本番ではSeederを実行しない。
- `OrderSeeder` の顧客名がすべて架空データであり、実顧客・家族・取引先などを識別できる情報を含まないことを確認する。
- 本番では開発用Seederを禁止し、起動・デプロイスクリプトから意図せず実行されないことを確認する。
- 開発DBの既定ユーザー名・パスワードはローカル開発専用とし、本番では使用しない。
- rootとLaravel配下の環境ファイル、DBダンプ、ログ、キャッシュ、秘密鍵、認証設定ファイルがGit管理対象およびDockerイメージに入らないことを再確認する。
- GitHub等のリモート側シークレットスキャンが利用可能なら、push後の検知だけに依存せず、push前にもローカルスキャンを再実行する。

## 5. 監査時の検証結果

| 検証 | 結果 | 備考 |
|---|---|---|
| PHP構文検査 | 成功 | `app`、`config`、`database`、`routes`、`tests` 配下を確認 |
| Laravelルート確認 | 成功 | 出荷記録関連ルートが登録されることを確認 |
| TypeScript型検査 | 成功 | `tsc --noEmit` 成功 |
| ESLint | 成功、警告あり | エラー0件、警告60件 |
| Next.js本番build | 検証不能 | ローカルの依存状態がLinux用SWCで、macOS用SWCが不足していたため完走せず |
| Laravel Featureテスト | 3 pass / 12 fail | 失敗はDB未起動・接続不能によるもので、機能の成否は判定できない |
| PHPファイルの構文上の致命傷 | 検出なし | API・DB・本番構成上の問題は別途存在する |
| shell構文・production compose構文 | 成功 | 実環境での動作確認は未実施 |

Next.js buildとFeatureテストについては、コードが正しいという意味の成功結果ではない。依存・DBを安全に用意した再現可能な環境で再実行する必要がある。

## 6. 安全にpushするための再開手順

- [ ] P0項目の仕様と修正方針を確定する
- [ ] 適用済みマイグレーションのrename問題を解消する
- [ ] UNIQUE制約削除用の追加マイグレーションを用意する
- [ ] 新規DBと既存DBの両方で通常の `migrate` を確認する
- [ ] 直売・JA保存でレスポンス本文の成功判定を行い、失敗時に入力を保持する
- [ ] 商品明細を持たない直売等級の保持・移行方針を実装する
- [ ] 本番Next.jsのAPI URLをクリーンbuildで検証する
- [ ] 本番nginx経由でSanctumログイン一式を確認する
- [ ] `.dockerignore` と本番環境変数注入を整備し、環境ファイルがイメージに入らないことを確認する
- [ ] `restore-db.sh` の引数なし実行を修正・確認する
- [ ] UserSeederの固定アカウントが開発専用であることを確認する
- [ ] OrderSeederの顧客名がすべて架空であることを確認する
- [ ] 本番でSeederが実行されないことを確認する
- [ ] DBを安全に用意し、Featureテストを全件成功させる
- [ ] クリーンな依存状態でNext.js本番buildを成功させる
- [ ] 今回変更したフロントエンドファイルのESLint警告を確認する
- [ ] 機密情報、個人情報、DBダンプ、ログ、絶対パス、不要な生成物を再スキャンする
- [ ] push対象だけを明示してステージし直す
- [ ] `git diff --cached --check` で空白・競合マーカー等を確認する
- [ ] `git diff --cached --name-status` と `git diff --cached` を人手で確認する
- [ ] ステージ済みスナップショットに対して構文・型・テストを再実行する
- [ ] SSH認証を復旧し、`git fetch` でリモート更新がないことを確認する
- [ ] `origin/main` との差分と未pushコミット数を再確認する
- [ ] 最終監査でGO判定を得てからcommit・pushする

## 7. この監査で実施していない操作

この監査では、アプリケーションコード、設定、マイグレーション、Seeder、Git履歴を変更していない。また、commitおよびpushも実行していない。

本ファイルは、監査結果を次の作業セッションへ引き継ぐために作成した記録である。
